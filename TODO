Produce several types of output
  + one page for one lang and 12 fonts

Add more languages (may wait till next version)

Document in README (in work, but looking good)

Check dir new-module1 for CodeCharts for FreeFont

Move the TextBox module to PDF::GraphicsUtils

NO REUSE OF A LOADED FONT FOR NOW
  check with David again

=done:  Check clagnut.com for pangrams

=done Create the font lists for docs automatically based on the Vars hash:
  =done: + in option "help"
  =done+ in rakudocs: use Build.rakumod with an sbin/install script

=done: Add a lang option that gens a page for one language 
       for all fonts

=done: Add an option to resolve font aliases or bail with info
       (check in FontFactory code)

=done
2025-08-11
IMPORTANT
1. Combine ALL primary and alias keys into ONE
   giant hash.


See my unpublished Font::Utils for FontFreeFace
  for using David's Font::FreeType

create a font sample sheet
 - See FontFactory

In docs, mention lowercase and numbers for aliases

=done:
2025-08-04:

# used FFT1 and added more special aliases
# tested codes and aliases to ensure no overlaps
Fix problem with alias names.
  - consider using the system FontFactory::Type1 uses

Add Numeric aliases

Ensure users do NOT use uppercase codes or aliases
